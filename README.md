# Stone-Paper-Scissors-with-AI
A simple Python-based Stone–Paper–Scissors game built using functions and the random module. The program runs through a menu-driven interface, allowing users to play multiple rounds. Each round generates a random opponent move and displays the result (win, lose, or draw) instantly.
